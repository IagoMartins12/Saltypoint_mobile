import {StyleSheet, Text, View} from 'react-native';

interface teste {
  testee?: string;
}
const Ajuda: React.FC<teste> = ({testee}) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default Ajuda;
